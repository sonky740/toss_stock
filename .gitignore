.DS_Store

# 에디터/IDE 로컬 설정
.vscode/

# 토스 Open API 자격증명·토큰 — 실수로 레포에 복사돼도 커밋되지 않도록 방어
auth.env
*.env
token.json

# SwiftPM / 네이티브 앱 빌드 산출물
tossstock-app/.build/
tossstock-app/build/

# 앱 아이콘 중간 산출물 (AppIcon.icns 는 make-icon.swift 로 재생성 — 커밋 대상)
tossstock-app/Packaging/AppIcon.iconset/
